%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:places, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:places, :new)

.tabs-content
  = form_for :place, url(:places, :create), :class => 'form-horizontal' do |f|
    =partial 'places/form', :locals => { :f => f }
